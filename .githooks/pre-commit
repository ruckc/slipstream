#!/usr/bin/env bash
set -euo pipefail

mise run lint

# cargo commands during lint may rewrite Cargo.lock (e.g. version bump not yet reflected).
# Stage it automatically so it travels with the commit rather than being left behind.
git diff --quiet apps/agent/Cargo.lock || git add apps/agent/Cargo.lock
