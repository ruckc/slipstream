[advisories]
# RUSTSEC-2023-0071: rsa crate Marvin Attack (timing side-channel in PKCS#1 decryption).
# The agent uses jsonwebtoken for RS256 *verification* only — the decrypt path is never
# called. No patched version of the rsa crate exists upstream as of 2026-06.
ignore = ["RUSTSEC-2023-0071"]
