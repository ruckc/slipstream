#!/usr/bin/env bash
#MISE description="Format all Rust crates with rustfmt"

REPO_ROOT="$(git rev-parse --show-toplevel)"

for crate in apps/agent apps/metrics-collector apps/harbor-collector; do
  (cd "$REPO_ROOT/$crate" && cargo fmt)
done
